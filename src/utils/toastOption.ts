import { ToastOptions } from 'react-toastify';

export const succesToast: ToastOptions = {
  position: 'top-center',
  autoClose: 3000,
  hideProgressBar: false,
  pauseOnHover: true,
  draggable: true,
  progress: undefined,
  theme: 'light',
};
